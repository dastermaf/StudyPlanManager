version: '3'

vars:
  TSC: './node_modules/.bin/tsc'

tasks:
  build:
    cmds:
      - '{{.TSC}}'

  test:
    deps: [build]
    cmds:
      - node -r dotenv/config dist/main.js dotenv_config_path=local.env